[package]
name = "misyks-beta-frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { workspace = true }
# Lo compartido con la aplicacion de control: acceso a datos, invocacion del
# Backend, configuracion local y estilos.
nucleo = { path = "../nucleo" }
